※ グローバル方針 `~/.claude/CLAUDE.md` に準拠。

# このリポジトリの成果物出力規約（スクレイピング）

- スクレイピング結果・レポート・分析は、原則として**自己完結型の HTML**（CSS / JS を inline、図解は SVG、画像は data URI で埋め込み）として出力する。
- 出力先は `reports/` または `output/`。プレビューで「見て」判断でき、そのまま引き継ぎ資料になる形にする。
- 長文のテキストログではなく、集計・比較・可視化を HTML で示す。単純作業でテキストを読む余裕がある場合のみ Markdown でも可。
